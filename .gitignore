# --- Python ---
.venv/
__pycache__/
*.py[cod]

# --- Scraper working dirs ---
# on-disk HTTP cache (scraper)
.cache/
# raw HTML audit dump (scraper)
_raw/
# scraper self-test scratch dir
scraper/_selftest/

# --- Build output ---
# site/ is committed so Vercel serves it as a prebuilt static deploy (see vercel.json).
# Re-run build.sh and re-commit site/ after changing scraped content.

# --- Node ---
node_modules/

# --- Deploy CLI scratch ---
.vercel/
.netlify/

# --- Backups / temp ---
*.bak

# --- OS / editor ---
.DS_Store
