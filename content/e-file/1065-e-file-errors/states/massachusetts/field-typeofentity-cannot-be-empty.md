# E-file error: Field 'TypeOfEntity' cannot be empty

```
Field 'TypeOfEntity' cannot be empty
```

enter cause and intro (e.g. "This happens when" or "You get this error when")

## Solution

enter solution here