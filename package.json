{
  "name": "gosystem-help",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Static search portal for GoSystem Tax RS help articles (Pagefind full-text search + error-code index).",
  "license": "UNLICENSED",
  "scripts": {
    "build": "bash build.sh",
    "index": "pagefind --site site",
    "test": "node search/matcher_selftest.mjs && node search/integration_selftest.mjs"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
